@model PhotoShare.Models.Album

@{
    ViewBag.Title = "Index";
}


<h1>@Model.Name</h1>

@foreach (var item in @Model.Photos)
{

    @Html.Partial("_Photo", item);

}

@Html.ActionLink("New Photo", "Create", new { albumId = @Model.ID })